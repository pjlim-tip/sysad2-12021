---
# tasks file for ubuntu-maria
- name: installing mariadb
  apt:
    name:
      - mariadb-server
      - mariadb-client
    state: present
    update_cache: yes

- name: starting mariadb
  systemd:
    name: mariadb
    state: started
    enabled: yes
